active_flow: build_epic # building e01 via build-epic cycle
workflow_mode: yolo # full auto-run through end of e01 (user requested)
active_epic_id: e01
active_story_id: e01s07 # BigBase-style footer (next) — e01s06 released to main
completed_epic: false
project_cycle:
  current_phase: build # discover → elaborate → plan → build → deploy → support
  completed_phases:
    - 1 # discover
    - 2 # elaborate
    - 3 # plan
  next_skill: build-epic
epic_cycle:
  # e01s06 released to main. Ready for e01s07 (BigBase footer).
  current_step: 2 # fast-mode: kickoff-branch (survey+plan done)
  next_skill: kickoff-branch
  story_bcps: 1
  completed_steps: [1]
  fast_mode: true # yolo: coalescing read-only steps
  audit_result: pass
  last_story_done: e01s06 # released to main as 598212c
stack:
  package_manager: npm # pure npm — package-lock.json
  site_dir: website # Astro site lives here
  versioning: manual # bumped by hand in website/package.json (no semantic-release)
release:
  target_version: 1.0.0
  current_version: 0.1.0 # website/package.json
  last_tag: null
  last_publish: null
  ci_verified: true # CI green for e01s05 integration test fix
git:
  branch: main
  repo: danielvm-git/big-news
  default_branch: main
  hash: "598212c"
  pushed: true
  contributors: "danielvm-git only (no Claude attribution)"
  protection: "force-push + deletion blocked; linear history required; direct commits (no PR gate)"
upstream:
  source: danielvm-git/astrobiologia
  relicense: "MIT relicensing authorized — maintainer owns upstream; upstream ships no LICENSE"
handoff:
  last_step_completed: >
    e01s06 released to main as 598212c.
    feat(core): add structured logging middleware and zod input validation.
    10 files, 383 insertions. Preflight gate: typecheck(0), format:check(pass),
    unit(30/30), build(pass). e01 is 6/7 stories done.
  next_action: "start e01s07 (BigBase-style footer) — next story in e01"
  next_skill: build-epic

active_flow: build_epic # building e01 via build-epic cycle
workflow_mode: yolo # full auto-run through end of e01 (user requested)
active_epic_id: e02
active_story_id: null
completed_epic: true # e01 all 7 stories done, archived
project_cycle:
  current_phase: build # discover → elaborate → plan → build → deploy → support
  completed_phases:
    - 1 # discover
    - 2 # elaborate
    - 3 # plan
  next_skill: build-epic
epic_cycle:
  # e01 complete. Ready for e02 (PostgreSQL Data Layer).
  current_step: 0 # needs slice-tasks + plan-work first
  next_skill: survey-context
  story_bcps: 0
  completed_steps: []
  fast_mode: false
  audit_result: null
  last_story_done: e01s07 # released to main as 114734e
stack:
  package_manager: npm # pure npm — package-lock.json
  site_dir: website # Astro site lives here
  versioning: manual # bumped by hand in website/package.json (no semantic-release)
release:
  target_version: 1.0.0
  current_version: 0.1.0 # website/package.json
  last_tag: null
  last_publish: null
  ci_verified: true # CI green for e01s05 integration test fix
git:
  branch: main
  repo: danielvm-git/big-news
  default_branch: main
  hash: "114734e"
  pushed: true
  contributors: "danielvm-git only (no Claude attribution)"
  protection: "force-push + deletion blocked; linear history required; direct commits (no PR gate)"
upstream:
  source: danielvm-git/astrobiologia
  relicense: "MIT relicensing authorized — maintainer owns upstream; upstream ships no LICENSE"
handoff:
  last_step_completed: >
    e01 complete! All 7 stories shipped:
    s01: Astro 5 scaffold + i18n
    s02: Dev toolchain (Husky, commitlint, Prettier)
    s03: Conventions & specs
    s04: License, repo, CI
    s05: Test infrastructure
    s06: Observability & validation (logger, middleware, zod)
    s07: BigBase-style footer
    Epic capsule archived to specs/epics/archive/e01-foundation-scaffold.
  next_action: "slice-tasks + plan-work for e02 (PostgreSQL Data Layer)"
  next_skill: slice-tasks
  notes: "e02 capsule_dir not yet created — needs slice-tasks + plan-work before build-epic"

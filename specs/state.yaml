active_flow: build_epic # building e01 via build-epic cycle
workflow_mode: interactive # human checkpoint per step (not yolo)
active_epic_id: e01
active_story_id: e01s01
completed_epic: false
project_cycle:
  current_phase: build # discover → elaborate → plan → build → deploy → support
  completed_phases:
    - 1 # discover
    - 2 # elaborate
    - 3 # plan
  next_skill: build-epic
epic_cycle:
  current_step: 6 # audit-code PASS; next: commit-message (step 7)
  next_skill: commit-message
  story_bcps: 1 # e01s01 is 1 BCP
  completed_steps: [1, 2, 3, 4, 5, 6] # survey, plan, kickoff, develop-tdd, verify-work, audit-code
  fast_mode: false
  audit_result: pass
stack:
  package_manager: npm # pure npm — package-lock.json
  site_dir: website # Astro site lives here
  versioning: manual # bumped by hand in website/package.json (no semantic-release)
release:
  target_version: 1.0.0
  current_version: 0.1.0 # website/package.json
  last_tag: null
  last_publish: null
  ci_verified: true # first CI run green on recreated repo
git:
  branch: main
  repo: danielvm-git/big-news
  default_branch: main
  hash: "14cfe64"
  pushed: true
  contributors: "danielvm-git only (no Claude attribution)"
  protection: "force-push + deletion blocked; linear history required; direct commits (no PR gate)"
upstream:
  source: danielvm-git/astrobiologia
  relicense: "MIT relicensing authorized — maintainer owns upstream; upstream ships no LICENSE"
handoff:
  last_step_completed: >
    Repo recreated clean after old one was deleted. Single foundation commit
    authored solely by danielvm-git (NO Co-Authored-By: Claude — that trailer had
    added "claude" to contributors). First CI run green; contributors = danielvm-git
    only; branch protection re-applied. Pure-npm Astro site in website/ (v0.1.0),
    Husky pre-push gate mirrors CI, commitlint + lint-staged hooks, pinned CI
    workflow. Integration/E2E CI lanes dormant until e02/e04 add code to test.
  next_action: "continue e01 feature stories on website/: i18n routing, React island, Tailwind v4, structured logger + zod validation (ADR-0018/0015), BigBase footer"

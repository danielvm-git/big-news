active_flow: release_branch
workflow_mode: yolo
active_epic_id: e08
active_story_id: e08s02
completed_epic: true
project_cycle:
  current_phase: build # discover → elaborate → plan → build → deploy → support
  completed_phases:
    - 1 # discover
    - 2 # elaborate
    - 3 # plan
  next_skill: build-epic
epic_cycle:
  # e08s02 complete! Next: e09 — Support
  current_step: 1
  next_skill: survey-context
  story_bcps: 3
  completed_steps: []
  fast_mode: false
  audit_result: pass
  last_story_done: e08s02
  next_story: e09s01
stack:
  package_manager: npm # pure npm — package-lock.json
  site_dir: website # Astro site lives here
  versioning: manual # bumped by hand in website/package.json (no semantic-release)
release:
  target_version: 1.0.0
  current_version: 0.1.0 # website/package.json
  last_tag: null
  last_publish: null
  ci_verified: true # CI green for e01s05 integration test fix
git:
  branch: main
  repo: danielvm-git/big-news
  default_branch: main
  hash: "55ec74a"
  pushed: true
  contributors: "danielvm-git only (no Claude attribution)"
  protection: "force-push + deletion blocked; linear history required; direct commits (no PR gate)"
upstream:
  source: danielvm-git/astrobiologia
  relicense: "MIT relicensing authorized — maintainer owns upstream; upstream ships no LICENSE"
handoff:
  e02_planned: >
    e02 capsule created at specs/epics/e02-postgres-data-layer/ — epic.yaml manifest (7 stories,
    8 BCPs). Build order: s01 interface → s02 connection → s03 schema/migrations →
    [s04 article ‖ s05 settings ‖ s06 storage ‖ s07 sanitizer].
  last_step_completed: >
    e08s02 complete! Database seed script shipped:
    - Modularized helper functions under 20 lines
    - Added support for env overrides for admin credentials
    - Wrapped database seeding operations in a transaction block
    - Optimized test teardown to perform bulk deletes (fixed timeouts)
    - Fixed test environment pollution in connection tests
    Released to main via release-branch cycle.
  next_action: "survey-context"
  next_skill: survey-context
  notes: "e08s02 complete! All unit and integration tests passing. CI verified."

---
import PageLayout from '../layouts/PageLayout.astro';
import { t } from '../lib/i18n';
---

<PageLayout title={t('nav.categories', 'en')}>
  <slot name="header"><h1>{t('nav.categories', 'en')}</h1></slot>
  <p>{t('category.all', 'en')}</p>
</PageLayout>

---
import PageLayout from '../layouts/PageLayout.astro';
import { t } from '../lib/i18n';
---

<PageLayout title={t('error.not_found', 'en')}>
  <slot name="header"><h1>{t('error.not_found', 'en')}</h1></slot>
  <p>{t('error.not_found_desc', 'en')}</p>
  <a href="/">{t('nav.home', 'en')}</a>
</PageLayout>

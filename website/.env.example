# PostgreSQL connection string
# Supports any Postgres provider: Neon, Supabase, BigBase, stand-alone, etc.
# Use the non-pooled endpoint (without -pooler) for advisory lock support.
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

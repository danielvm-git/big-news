services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: bignews_test
      POSTGRES_USER: bignews
      POSTGRES_PASSWORD: bignews
    ports:
      - '5433:5432'
    tmpfs:
      - /var/lib/postgresql/data

{
  "name": "big-news-root",
  "private": true,
  "type": "module",
  "description": "Repository wrapper for big-news. The Astro site lives in website/. This root holds only git-workflow tooling (Husky, commitlint, lint-staged, Prettier).",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "prepare": "husky",
    "preflight": "cd website && npm run preflight"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.2",
    "@commitlint/config-conventional": "^21.0.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.0",
    "prettier": "^3.4.2",
    "prettier-plugin-astro": "^0.14.1"
  }
}
